fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
httpx>=0.27,<1
jinja2>=3.1,<4
# pyarrow volontairement absent : son import s'est bloqué dans le conteneur (collecte figée,
# aucune trace). La collecte LMArena passe par l'API /rows, plus lente mais sans dépendance.
